<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:id="@+id/bottom_sheet_row"
    style="@style/BottomSheetLinearRow">

    <ImageView
        android:id="@+id/bottom_sheet_row_ic"
        android:src="@drawable/ic_edit_black_24dp"
        android:contentDescription="@string/ic_descrip"
        style="@style/BottomSheetImage"/>
    <TextView
        android:id="@+id/bottom_sheet_row_title"
        android:text="@string/default_name"
        style="@style/BottomSheetText" />

</LinearLayout>